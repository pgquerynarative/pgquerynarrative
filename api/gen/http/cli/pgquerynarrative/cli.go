// Code generated by goa v3.24.1, DO NOT EDIT.
//
// pgquerynarrative HTTP client CLI support package
//
// Command:
// $ goa gen github.com/pgquerynarrative/pgquerynarrative/api/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	connectionsc "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/connections/client"
	dashboardsc "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/dashboards/client"
	investigationsc "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/investigations/client"
	queriesc "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/queries/client"
	reportsc "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/reports/client"
	schedulesc "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/schedules/client"
	schemac "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/schema/client"
	suggestionsc "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/suggestions/client"
	workspacec "github.com/pgquerynarrative/pgquerynarrative/api/gen/http/workspace/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"connections list",
		"schema get",
		"suggestions (queries|questions|similar|ask|chat|explain)",
		"reports (generate|get|list|similar|rewrite|create-share|get-shared|list-shares|revoke-share)",
		"schedules (list|create|update|delete|run-now|list-runs|retry-run|list-deliveries)",
		"dashboards (list|create|get|update|delete|resolve)",
		"queries (run|stat-statements|explain-plan|compare-plans|list-saved|save|get-saved|delete-saved)",
		"investigations (create|create-from-regression|list|get|add-candidate|update-fix|suggest-rewrite|rank-candidates|generate-report)",
		"workspace (overview|regressions|acknowledge-regression|demo-scenarios|security-trust)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "connections list" + "\n" +
		os.Args[0] + " " + "schema get --connection-id \"Quam asperiores placeat cupiditate.\"" + "\n" +
		os.Args[0] + " " + "suggestions queries --intent \"Mollitia voluptatum omnis omnis.\" --limit 15" + "\n" +
		os.Args[0] + " " + "reports generate --body '{\n      \"connection_id\": \"Sit sit dolorem quia at earum iste.\",\n      \"saved_query_id\": \"311d6455-9936-43d4-9608-50c2d0cedfe2\",\n      \"sql\": \"5x\"\n   }'" + "\n" +
		os.Args[0] + " " + "schedules list" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		connectionsFlags = flag.NewFlagSet("connections", flag.ContinueOnError)

		connectionsListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		schemaFlags = flag.NewFlagSet("schema", flag.ContinueOnError)

		schemaGetFlags            = flag.NewFlagSet("get", flag.ExitOnError)
		schemaGetConnectionIDFlag = schemaGetFlags.String("connection-id", "", "")

		suggestionsFlags = flag.NewFlagSet("suggestions", flag.ContinueOnError)

		suggestionsQueriesFlags      = flag.NewFlagSet("queries", flag.ExitOnError)
		suggestionsQueriesIntentFlag = suggestionsQueriesFlags.String("intent", "", "")
		suggestionsQueriesLimitFlag  = suggestionsQueriesFlags.String("limit", "5", "")

		suggestionsQuestionsFlags            = flag.NewFlagSet("questions", flag.ExitOnError)
		suggestionsQuestionsConnectionIDFlag = suggestionsQuestionsFlags.String("connection-id", "", "")
		suggestionsQuestionsLimitFlag        = suggestionsQuestionsFlags.String("limit", "8", "")

		suggestionsSimilarFlags     = flag.NewFlagSet("similar", flag.ExitOnError)
		suggestionsSimilarTextFlag  = suggestionsSimilarFlags.String("text", "", "")
		suggestionsSimilarLimitFlag = suggestionsSimilarFlags.String("limit", "5", "")

		suggestionsAskFlags    = flag.NewFlagSet("ask", flag.ExitOnError)
		suggestionsAskBodyFlag = suggestionsAskFlags.String("body", "REQUIRED", "")

		suggestionsChatFlags    = flag.NewFlagSet("chat", flag.ExitOnError)
		suggestionsChatBodyFlag = suggestionsChatFlags.String("body", "REQUIRED", "")

		suggestionsExplainFlags    = flag.NewFlagSet("explain", flag.ExitOnError)
		suggestionsExplainBodyFlag = suggestionsExplainFlags.String("body", "REQUIRED", "")

		reportsFlags = flag.NewFlagSet("reports", flag.ContinueOnError)

		reportsGenerateFlags    = flag.NewFlagSet("generate", flag.ExitOnError)
		reportsGenerateBodyFlag = reportsGenerateFlags.String("body", "REQUIRED", "")

		reportsGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		reportsGetIDFlag = reportsGetFlags.String("id", "REQUIRED", "")

		reportsListFlags            = flag.NewFlagSet("list", flag.ExitOnError)
		reportsListSavedQueryIDFlag = reportsListFlags.String("saved-query-id", "", "")
		reportsListConnectionIDFlag = reportsListFlags.String("connection-id", "", "")
		reportsListLimitFlag        = reportsListFlags.String("limit", "50", "")
		reportsListOffsetFlag       = reportsListFlags.String("offset", "", "")

		reportsSimilarFlags            = flag.NewFlagSet("similar", flag.ExitOnError)
		reportsSimilarTextFlag         = reportsSimilarFlags.String("text", "REQUIRED", "")
		reportsSimilarConnectionIDFlag = reportsSimilarFlags.String("connection-id", "", "")
		reportsSimilarLimitFlag        = reportsSimilarFlags.String("limit", "5", "")

		reportsRewriteFlags    = flag.NewFlagSet("rewrite", flag.ExitOnError)
		reportsRewriteBodyFlag = reportsRewriteFlags.String("body", "REQUIRED", "")

		reportsCreateShareFlags    = flag.NewFlagSet("create-share", flag.ExitOnError)
		reportsCreateShareBodyFlag = reportsCreateShareFlags.String("body", "REQUIRED", "")

		reportsGetSharedFlags     = flag.NewFlagSet("get-shared", flag.ExitOnError)
		reportsGetSharedTokenFlag = reportsGetSharedFlags.String("token", "REQUIRED", "")

		reportsListSharesFlags        = flag.NewFlagSet("list-shares", flag.ExitOnError)
		reportsListSharesReportIDFlag = reportsListSharesFlags.String("report-id", "REQUIRED", "")

		reportsRevokeShareFlags  = flag.NewFlagSet("revoke-share", flag.ExitOnError)
		reportsRevokeShareIDFlag = reportsRevokeShareFlags.String("id", "REQUIRED", "Share link ID (not the token)")

		schedulesFlags = flag.NewFlagSet("schedules", flag.ContinueOnError)

		schedulesListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		schedulesCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		schedulesCreateBodyFlag = schedulesCreateFlags.String("body", "REQUIRED", "")

		schedulesUpdateFlags    = flag.NewFlagSet("update", flag.ExitOnError)
		schedulesUpdateBodyFlag = schedulesUpdateFlags.String("body", "REQUIRED", "")
		schedulesUpdateIDFlag   = schedulesUpdateFlags.String("id", "REQUIRED", "")

		schedulesDeleteFlags  = flag.NewFlagSet("delete", flag.ExitOnError)
		schedulesDeleteIDFlag = schedulesDeleteFlags.String("id", "REQUIRED", "")

		schedulesRunNowFlags  = flag.NewFlagSet("run-now", flag.ExitOnError)
		schedulesRunNowIDFlag = schedulesRunNowFlags.String("id", "REQUIRED", "")

		schedulesListRunsFlags  = flag.NewFlagSet("list-runs", flag.ExitOnError)
		schedulesListRunsIDFlag = schedulesListRunsFlags.String("id", "REQUIRED", "")

		schedulesRetryRunFlags     = flag.NewFlagSet("retry-run", flag.ExitOnError)
		schedulesRetryRunRunIDFlag = schedulesRetryRunFlags.String("run-id", "REQUIRED", "")

		schedulesListDeliveriesFlags = flag.NewFlagSet("list-deliveries", flag.ExitOnError)

		dashboardsFlags = flag.NewFlagSet("dashboards", flag.ContinueOnError)

		dashboardsListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		dashboardsCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		dashboardsCreateBodyFlag = dashboardsCreateFlags.String("body", "REQUIRED", "")

		dashboardsGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		dashboardsGetIDFlag = dashboardsGetFlags.String("id", "REQUIRED", "")

		dashboardsUpdateFlags    = flag.NewFlagSet("update", flag.ExitOnError)
		dashboardsUpdateBodyFlag = dashboardsUpdateFlags.String("body", "REQUIRED", "")
		dashboardsUpdateIDFlag   = dashboardsUpdateFlags.String("id", "REQUIRED", "")

		dashboardsDeleteFlags  = flag.NewFlagSet("delete", flag.ExitOnError)
		dashboardsDeleteIDFlag = dashboardsDeleteFlags.String("id", "REQUIRED", "")

		dashboardsResolveFlags  = flag.NewFlagSet("resolve", flag.ExitOnError)
		dashboardsResolveIDFlag = dashboardsResolveFlags.String("id", "REQUIRED", "")

		queriesFlags = flag.NewFlagSet("queries", flag.ContinueOnError)

		queriesRunFlags    = flag.NewFlagSet("run", flag.ExitOnError)
		queriesRunBodyFlag = queriesRunFlags.String("body", "REQUIRED", "")

		queriesStatStatementsFlags            = flag.NewFlagSet("stat-statements", flag.ExitOnError)
		queriesStatStatementsOrderByFlag      = queriesStatStatementsFlags.String("order-by", "total_time", "")
		queriesStatStatementsLimitFlag        = queriesStatStatementsFlags.String("limit", "20", "")
		queriesStatStatementsConnectionIDFlag = queriesStatStatementsFlags.String("connection-id", "", "")

		queriesExplainPlanFlags    = flag.NewFlagSet("explain-plan", flag.ExitOnError)
		queriesExplainPlanBodyFlag = queriesExplainPlanFlags.String("body", "REQUIRED", "")

		queriesComparePlansFlags    = flag.NewFlagSet("compare-plans", flag.ExitOnError)
		queriesComparePlansBodyFlag = queriesComparePlansFlags.String("body", "REQUIRED", "")

		queriesListSavedFlags            = flag.NewFlagSet("list-saved", flag.ExitOnError)
		queriesListSavedTagsFlag         = queriesListSavedFlags.String("tags", "", "")
		queriesListSavedConnectionIDFlag = queriesListSavedFlags.String("connection-id", "", "")
		queriesListSavedLimitFlag        = queriesListSavedFlags.String("limit", "50", "")
		queriesListSavedOffsetFlag       = queriesListSavedFlags.String("offset", "", "")

		queriesSaveFlags    = flag.NewFlagSet("save", flag.ExitOnError)
		queriesSaveBodyFlag = queriesSaveFlags.String("body", "REQUIRED", "")

		queriesGetSavedFlags  = flag.NewFlagSet("get-saved", flag.ExitOnError)
		queriesGetSavedIDFlag = queriesGetSavedFlags.String("id", "REQUIRED", "")

		queriesDeleteSavedFlags  = flag.NewFlagSet("delete-saved", flag.ExitOnError)
		queriesDeleteSavedIDFlag = queriesDeleteSavedFlags.String("id", "REQUIRED", "")

		investigationsFlags = flag.NewFlagSet("investigations", flag.ContinueOnError)

		investigationsCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		investigationsCreateBodyFlag = investigationsCreateFlags.String("body", "REQUIRED", "")

		investigationsCreateFromRegressionFlags    = flag.NewFlagSet("create-from-regression", flag.ExitOnError)
		investigationsCreateFromRegressionBodyFlag = investigationsCreateFromRegressionFlags.String("body", "REQUIRED", "")

		investigationsListFlags      = flag.NewFlagSet("list", flag.ExitOnError)
		investigationsListLimitFlag  = investigationsListFlags.String("limit", "20", "")
		investigationsListOffsetFlag = investigationsListFlags.String("offset", "", "")

		investigationsGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		investigationsGetIDFlag = investigationsGetFlags.String("id", "REQUIRED", "")

		investigationsAddCandidateFlags    = flag.NewFlagSet("add-candidate", flag.ExitOnError)
		investigationsAddCandidateBodyFlag = investigationsAddCandidateFlags.String("body", "REQUIRED", "")
		investigationsAddCandidateIDFlag   = investigationsAddCandidateFlags.String("id", "REQUIRED", "")

		investigationsUpdateFixFlags    = flag.NewFlagSet("update-fix", flag.ExitOnError)
		investigationsUpdateFixBodyFlag = investigationsUpdateFixFlags.String("body", "REQUIRED", "")
		investigationsUpdateFixIDFlag   = investigationsUpdateFixFlags.String("id", "REQUIRED", "")

		investigationsSuggestRewriteFlags  = flag.NewFlagSet("suggest-rewrite", flag.ExitOnError)
		investigationsSuggestRewriteIDFlag = investigationsSuggestRewriteFlags.String("id", "REQUIRED", "")

		investigationsRankCandidatesFlags    = flag.NewFlagSet("rank-candidates", flag.ExitOnError)
		investigationsRankCandidatesBodyFlag = investigationsRankCandidatesFlags.String("body", "REQUIRED", "")
		investigationsRankCandidatesIDFlag   = investigationsRankCandidatesFlags.String("id", "REQUIRED", "")

		investigationsGenerateReportFlags  = flag.NewFlagSet("generate-report", flag.ExitOnError)
		investigationsGenerateReportIDFlag = investigationsGenerateReportFlags.String("id", "REQUIRED", "")

		workspaceFlags = flag.NewFlagSet("workspace", flag.ContinueOnError)

		workspaceOverviewFlags = flag.NewFlagSet("overview", flag.ExitOnError)

		workspaceRegressionsFlags                   = flag.NewFlagSet("regressions", flag.ExitOnError)
		workspaceRegressionsLimitFlag               = workspaceRegressionsFlags.String("limit", "10", "")
		workspaceRegressionsIncludeAcknowledgedFlag = workspaceRegressionsFlags.String("include-acknowledged", "", "")

		workspaceAcknowledgeRegressionFlags  = flag.NewFlagSet("acknowledge-regression", flag.ExitOnError)
		workspaceAcknowledgeRegressionIDFlag = workspaceAcknowledgeRegressionFlags.String("id", "REQUIRED", "")

		workspaceDemoScenariosFlags = flag.NewFlagSet("demo-scenarios", flag.ExitOnError)

		workspaceSecurityTrustFlags            = flag.NewFlagSet("security-trust", flag.ExitOnError)
		workspaceSecurityTrustConnectionIDFlag = workspaceSecurityTrustFlags.String("connection-id", "", "")
	)
	connectionsFlags.Usage = connectionsUsage
	connectionsListFlags.Usage = connectionsListUsage

	schemaFlags.Usage = schemaUsage
	schemaGetFlags.Usage = schemaGetUsage

	suggestionsFlags.Usage = suggestionsUsage
	suggestionsQueriesFlags.Usage = suggestionsQueriesUsage
	suggestionsQuestionsFlags.Usage = suggestionsQuestionsUsage
	suggestionsSimilarFlags.Usage = suggestionsSimilarUsage
	suggestionsAskFlags.Usage = suggestionsAskUsage
	suggestionsChatFlags.Usage = suggestionsChatUsage
	suggestionsExplainFlags.Usage = suggestionsExplainUsage

	reportsFlags.Usage = reportsUsage
	reportsGenerateFlags.Usage = reportsGenerateUsage
	reportsGetFlags.Usage = reportsGetUsage
	reportsListFlags.Usage = reportsListUsage
	reportsSimilarFlags.Usage = reportsSimilarUsage
	reportsRewriteFlags.Usage = reportsRewriteUsage
	reportsCreateShareFlags.Usage = reportsCreateShareUsage
	reportsGetSharedFlags.Usage = reportsGetSharedUsage
	reportsListSharesFlags.Usage = reportsListSharesUsage
	reportsRevokeShareFlags.Usage = reportsRevokeShareUsage

	schedulesFlags.Usage = schedulesUsage
	schedulesListFlags.Usage = schedulesListUsage
	schedulesCreateFlags.Usage = schedulesCreateUsage
	schedulesUpdateFlags.Usage = schedulesUpdateUsage
	schedulesDeleteFlags.Usage = schedulesDeleteUsage
	schedulesRunNowFlags.Usage = schedulesRunNowUsage
	schedulesListRunsFlags.Usage = schedulesListRunsUsage
	schedulesRetryRunFlags.Usage = schedulesRetryRunUsage
	schedulesListDeliveriesFlags.Usage = schedulesListDeliveriesUsage

	dashboardsFlags.Usage = dashboardsUsage
	dashboardsListFlags.Usage = dashboardsListUsage
	dashboardsCreateFlags.Usage = dashboardsCreateUsage
	dashboardsGetFlags.Usage = dashboardsGetUsage
	dashboardsUpdateFlags.Usage = dashboardsUpdateUsage
	dashboardsDeleteFlags.Usage = dashboardsDeleteUsage
	dashboardsResolveFlags.Usage = dashboardsResolveUsage

	queriesFlags.Usage = queriesUsage
	queriesRunFlags.Usage = queriesRunUsage
	queriesStatStatementsFlags.Usage = queriesStatStatementsUsage
	queriesExplainPlanFlags.Usage = queriesExplainPlanUsage
	queriesComparePlansFlags.Usage = queriesComparePlansUsage
	queriesListSavedFlags.Usage = queriesListSavedUsage
	queriesSaveFlags.Usage = queriesSaveUsage
	queriesGetSavedFlags.Usage = queriesGetSavedUsage
	queriesDeleteSavedFlags.Usage = queriesDeleteSavedUsage

	investigationsFlags.Usage = investigationsUsage
	investigationsCreateFlags.Usage = investigationsCreateUsage
	investigationsCreateFromRegressionFlags.Usage = investigationsCreateFromRegressionUsage
	investigationsListFlags.Usage = investigationsListUsage
	investigationsGetFlags.Usage = investigationsGetUsage
	investigationsAddCandidateFlags.Usage = investigationsAddCandidateUsage
	investigationsUpdateFixFlags.Usage = investigationsUpdateFixUsage
	investigationsSuggestRewriteFlags.Usage = investigationsSuggestRewriteUsage
	investigationsRankCandidatesFlags.Usage = investigationsRankCandidatesUsage
	investigationsGenerateReportFlags.Usage = investigationsGenerateReportUsage

	workspaceFlags.Usage = workspaceUsage
	workspaceOverviewFlags.Usage = workspaceOverviewUsage
	workspaceRegressionsFlags.Usage = workspaceRegressionsUsage
	workspaceAcknowledgeRegressionFlags.Usage = workspaceAcknowledgeRegressionUsage
	workspaceDemoScenariosFlags.Usage = workspaceDemoScenariosUsage
	workspaceSecurityTrustFlags.Usage = workspaceSecurityTrustUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "connections":
			svcf = connectionsFlags
		case "schema":
			svcf = schemaFlags
		case "suggestions":
			svcf = suggestionsFlags
		case "reports":
			svcf = reportsFlags
		case "schedules":
			svcf = schedulesFlags
		case "dashboards":
			svcf = dashboardsFlags
		case "queries":
			svcf = queriesFlags
		case "investigations":
			svcf = investigationsFlags
		case "workspace":
			svcf = workspaceFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "connections":
			switch epn {
			case "list":
				epf = connectionsListFlags

			}

		case "schema":
			switch epn {
			case "get":
				epf = schemaGetFlags

			}

		case "suggestions":
			switch epn {
			case "queries":
				epf = suggestionsQueriesFlags

			case "questions":
				epf = suggestionsQuestionsFlags

			case "similar":
				epf = suggestionsSimilarFlags

			case "ask":
				epf = suggestionsAskFlags

			case "chat":
				epf = suggestionsChatFlags

			case "explain":
				epf = suggestionsExplainFlags

			}

		case "reports":
			switch epn {
			case "generate":
				epf = reportsGenerateFlags

			case "get":
				epf = reportsGetFlags

			case "list":
				epf = reportsListFlags

			case "similar":
				epf = reportsSimilarFlags

			case "rewrite":
				epf = reportsRewriteFlags

			case "create-share":
				epf = reportsCreateShareFlags

			case "get-shared":
				epf = reportsGetSharedFlags

			case "list-shares":
				epf = reportsListSharesFlags

			case "revoke-share":
				epf = reportsRevokeShareFlags

			}

		case "schedules":
			switch epn {
			case "list":
				epf = schedulesListFlags

			case "create":
				epf = schedulesCreateFlags

			case "update":
				epf = schedulesUpdateFlags

			case "delete":
				epf = schedulesDeleteFlags

			case "run-now":
				epf = schedulesRunNowFlags

			case "list-runs":
				epf = schedulesListRunsFlags

			case "retry-run":
				epf = schedulesRetryRunFlags

			case "list-deliveries":
				epf = schedulesListDeliveriesFlags

			}

		case "dashboards":
			switch epn {
			case "list":
				epf = dashboardsListFlags

			case "create":
				epf = dashboardsCreateFlags

			case "get":
				epf = dashboardsGetFlags

			case "update":
				epf = dashboardsUpdateFlags

			case "delete":
				epf = dashboardsDeleteFlags

			case "resolve":
				epf = dashboardsResolveFlags

			}

		case "queries":
			switch epn {
			case "run":
				epf = queriesRunFlags

			case "stat-statements":
				epf = queriesStatStatementsFlags

			case "explain-plan":
				epf = queriesExplainPlanFlags

			case "compare-plans":
				epf = queriesComparePlansFlags

			case "list-saved":
				epf = queriesListSavedFlags

			case "save":
				epf = queriesSaveFlags

			case "get-saved":
				epf = queriesGetSavedFlags

			case "delete-saved":
				epf = queriesDeleteSavedFlags

			}

		case "investigations":
			switch epn {
			case "create":
				epf = investigationsCreateFlags

			case "create-from-regression":
				epf = investigationsCreateFromRegressionFlags

			case "list":
				epf = investigationsListFlags

			case "get":
				epf = investigationsGetFlags

			case "add-candidate":
				epf = investigationsAddCandidateFlags

			case "update-fix":
				epf = investigationsUpdateFixFlags

			case "suggest-rewrite":
				epf = investigationsSuggestRewriteFlags

			case "rank-candidates":
				epf = investigationsRankCandidatesFlags

			case "generate-report":
				epf = investigationsGenerateReportFlags

			}

		case "workspace":
			switch epn {
			case "overview":
				epf = workspaceOverviewFlags

			case "regressions":
				epf = workspaceRegressionsFlags

			case "acknowledge-regression":
				epf = workspaceAcknowledgeRegressionFlags

			case "demo-scenarios":
				epf = workspaceDemoScenariosFlags

			case "security-trust":
				epf = workspaceSecurityTrustFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "connections":
			c := connectionsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			}
		case "schema":
			c := schemac.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get":
				endpoint = c.Get()
				data, err = schemac.BuildGetPayload(*schemaGetConnectionIDFlag)
			}
		case "suggestions":
			c := suggestionsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "queries":
				endpoint = c.Queries()
				data, err = suggestionsc.BuildQueriesPayload(*suggestionsQueriesIntentFlag, *suggestionsQueriesLimitFlag)
			case "questions":
				endpoint = c.Questions()
				data, err = suggestionsc.BuildQuestionsPayload(*suggestionsQuestionsConnectionIDFlag, *suggestionsQuestionsLimitFlag)
			case "similar":
				endpoint = c.Similar()
				data, err = suggestionsc.BuildSimilarPayload(*suggestionsSimilarTextFlag, *suggestionsSimilarLimitFlag)
			case "ask":
				endpoint = c.Ask()
				data, err = suggestionsc.BuildAskPayload(*suggestionsAskBodyFlag)
			case "chat":
				endpoint = c.Chat()
				data, err = suggestionsc.BuildChatPayload(*suggestionsChatBodyFlag)
			case "explain":
				endpoint = c.Explain()
				data, err = suggestionsc.BuildExplainPayload(*suggestionsExplainBodyFlag)
			}
		case "reports":
			c := reportsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "generate":
				endpoint = c.Generate()
				data, err = reportsc.BuildGeneratePayload(*reportsGenerateBodyFlag)
			case "get":
				endpoint = c.Get()
				data, err = reportsc.BuildGetPayload(*reportsGetIDFlag)
			case "list":
				endpoint = c.List()
				data, err = reportsc.BuildListPayload(*reportsListSavedQueryIDFlag, *reportsListConnectionIDFlag, *reportsListLimitFlag, *reportsListOffsetFlag)
			case "similar":
				endpoint = c.Similar()
				data, err = reportsc.BuildSimilarPayload(*reportsSimilarTextFlag, *reportsSimilarConnectionIDFlag, *reportsSimilarLimitFlag)
			case "rewrite":
				endpoint = c.Rewrite()
				data, err = reportsc.BuildRewritePayload(*reportsRewriteBodyFlag)
			case "create-share":
				endpoint = c.CreateShare()
				data, err = reportsc.BuildCreateSharePayload(*reportsCreateShareBodyFlag)
			case "get-shared":
				endpoint = c.GetShared()
				data, err = reportsc.BuildGetSharedPayload(*reportsGetSharedTokenFlag)
			case "list-shares":
				endpoint = c.ListShares()
				data, err = reportsc.BuildListSharesPayload(*reportsListSharesReportIDFlag)
			case "revoke-share":
				endpoint = c.RevokeShare()
				data, err = reportsc.BuildRevokeSharePayload(*reportsRevokeShareIDFlag)
			}
		case "schedules":
			c := schedulesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "create":
				endpoint = c.Create()
				data, err = schedulesc.BuildCreatePayload(*schedulesCreateBodyFlag)
			case "update":
				endpoint = c.Update()
				data, err = schedulesc.BuildUpdatePayload(*schedulesUpdateBodyFlag, *schedulesUpdateIDFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = schedulesc.BuildDeletePayload(*schedulesDeleteIDFlag)
			case "run-now":
				endpoint = c.RunNow()
				data, err = schedulesc.BuildRunNowPayload(*schedulesRunNowIDFlag)
			case "list-runs":
				endpoint = c.ListRuns()
				data, err = schedulesc.BuildListRunsPayload(*schedulesListRunsIDFlag)
			case "retry-run":
				endpoint = c.RetryRun()
				data, err = schedulesc.BuildRetryRunPayload(*schedulesRetryRunRunIDFlag)
			case "list-deliveries":
				endpoint = c.ListDeliveries()
			}
		case "dashboards":
			c := dashboardsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "create":
				endpoint = c.Create()
				data, err = dashboardsc.BuildCreatePayload(*dashboardsCreateBodyFlag)
			case "get":
				endpoint = c.Get()
				data, err = dashboardsc.BuildGetPayload(*dashboardsGetIDFlag)
			case "update":
				endpoint = c.Update()
				data, err = dashboardsc.BuildUpdatePayload(*dashboardsUpdateBodyFlag, *dashboardsUpdateIDFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = dashboardsc.BuildDeletePayload(*dashboardsDeleteIDFlag)
			case "resolve":
				endpoint = c.Resolve()
				data, err = dashboardsc.BuildResolvePayload(*dashboardsResolveIDFlag)
			}
		case "queries":
			c := queriesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "run":
				endpoint = c.Run()
				data, err = queriesc.BuildRunPayload(*queriesRunBodyFlag)
			case "stat-statements":
				endpoint = c.StatStatements()
				data, err = queriesc.BuildStatStatementsPayload(*queriesStatStatementsOrderByFlag, *queriesStatStatementsLimitFlag, *queriesStatStatementsConnectionIDFlag)
			case "explain-plan":
				endpoint = c.ExplainPlan()
				data, err = queriesc.BuildExplainPlanPayload(*queriesExplainPlanBodyFlag)
			case "compare-plans":
				endpoint = c.ComparePlans()
				data, err = queriesc.BuildComparePlansPayload(*queriesComparePlansBodyFlag)
			case "list-saved":
				endpoint = c.ListSaved()
				data, err = queriesc.BuildListSavedPayload(*queriesListSavedTagsFlag, *queriesListSavedConnectionIDFlag, *queriesListSavedLimitFlag, *queriesListSavedOffsetFlag)
			case "save":
				endpoint = c.Save()
				data, err = queriesc.BuildSavePayload(*queriesSaveBodyFlag)
			case "get-saved":
				endpoint = c.GetSaved()
				data, err = queriesc.BuildGetSavedPayload(*queriesGetSavedIDFlag)
			case "delete-saved":
				endpoint = c.DeleteSaved()
				data, err = queriesc.BuildDeleteSavedPayload(*queriesDeleteSavedIDFlag)
			}
		case "investigations":
			c := investigationsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "create":
				endpoint = c.Create()
				data, err = investigationsc.BuildCreatePayload(*investigationsCreateBodyFlag)
			case "create-from-regression":
				endpoint = c.CreateFromRegression()
				data, err = investigationsc.BuildCreateFromRegressionPayload(*investigationsCreateFromRegressionBodyFlag)
			case "list":
				endpoint = c.List()
				data, err = investigationsc.BuildListPayload(*investigationsListLimitFlag, *investigationsListOffsetFlag)
			case "get":
				endpoint = c.Get()
				data, err = investigationsc.BuildGetPayload(*investigationsGetIDFlag)
			case "add-candidate":
				endpoint = c.AddCandidate()
				data, err = investigationsc.BuildAddCandidatePayload(*investigationsAddCandidateBodyFlag, *investigationsAddCandidateIDFlag)
			case "update-fix":
				endpoint = c.UpdateFix()
				data, err = investigationsc.BuildUpdateFixPayload(*investigationsUpdateFixBodyFlag, *investigationsUpdateFixIDFlag)
			case "suggest-rewrite":
				endpoint = c.SuggestRewrite()
				data, err = investigationsc.BuildSuggestRewritePayload(*investigationsSuggestRewriteIDFlag)
			case "rank-candidates":
				endpoint = c.RankCandidates()
				data, err = investigationsc.BuildRankCandidatesPayload(*investigationsRankCandidatesBodyFlag, *investigationsRankCandidatesIDFlag)
			case "generate-report":
				endpoint = c.GenerateReport()
				data, err = investigationsc.BuildGenerateReportPayload(*investigationsGenerateReportIDFlag)
			}
		case "workspace":
			c := workspacec.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "overview":
				endpoint = c.Overview()
			case "regressions":
				endpoint = c.Regressions()
				data, err = workspacec.BuildRegressionsPayload(*workspaceRegressionsLimitFlag, *workspaceRegressionsIncludeAcknowledgedFlag)
			case "acknowledge-regression":
				endpoint = c.AcknowledgeRegression()
				data, err = workspacec.BuildAcknowledgeRegressionPayload(*workspaceAcknowledgeRegressionIDFlag)
			case "demo-scenarios":
				endpoint = c.DemoScenarios()
			case "security-trust":
				endpoint = c.SecurityTrust()
				data, err = workspacec.BuildSecurityTrustPayload(*workspaceSecurityTrustConnectionIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// connectionsUsage displays the usage of the connections command and its
// subcommands.
func connectionsUsage() {
	fmt.Fprintln(os.Stderr, `Available read-only data connections`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] connections COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List configured data connections (id and display name only)`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s connections COMMAND --help\n", os.Args[0])
}
func connectionsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] connections list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List configured data connections (id and display name only)`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "connections list")
}

// schemaUsage displays the usage of the schema command and its subcommands.
func schemaUsage() {
	fmt.Fprintln(os.Stderr, `Database schema and catalog for allowed queryable objects`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] schema COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    get: Return the list of allowed schemas with their tables and columns (from information_schema, read-only).`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s schema COMMAND --help\n", os.Args[0])
}
func schemaGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schema get", os.Args[0])
	fmt.Fprint(os.Stderr, " -connection-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Return the list of allowed schemas with their tables and columns (from information_schema, read-only).`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -connection-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schema get --connection-id \"Quam asperiores placeat cupiditate.\"")
}

// suggestionsUsage displays the usage of the suggestions command and its
// subcommands.
func suggestionsUsage() {
	fmt.Fprintln(os.Stderr, `Query suggestions for MCP and other clients (curated examples and saved-query match by intent)`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] suggestions COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    queries: Return suggested SQL queries: curated examples plus saved queries matching optional intent.`)
	fmt.Fprintln(os.Stderr, `    questions: Suggest natural-language questions based on the current schema for discovery and onboarding.`)
	fmt.Fprintln(os.Stderr, `    similar: Return saved queries semantically similar to the given text (embedding-based). Requires embeddings to be enabled.`)
	fmt.Fprintln(os.Stderr, `    ask: Natural language to SQL and report in one step: ask a question, get a generated SELECT, run it, and return the narrative report. Requires LLM.`)
	fmt.Fprintln(os.Stderr, `    chat: Multi-turn Ask conversation with short context memory and optional follow-up suggestions.`)
	fmt.Fprintln(os.Stderr, `    explain: Explain a SQL query in plain English (one or two sentences). Requires LLM.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s suggestions COMMAND --help\n", os.Args[0])
}
func suggestionsQueriesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] suggestions queries", os.Args[0])
	fmt.Fprint(os.Stderr, " -intent STRING")
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Return suggested SQL queries: curated examples plus saved queries matching optional intent.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -intent STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "suggestions queries --intent \"Mollitia voluptatum omnis omnis.\" --limit 15")
}

func suggestionsQuestionsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] suggestions questions", os.Args[0])
	fmt.Fprint(os.Stderr, " -connection-id STRING")
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Suggest natural-language questions based on the current schema for discovery and onboarding.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -connection-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "suggestions questions --connection-id \"Laudantium repudiandae.\" --limit 19")
}

func suggestionsSimilarUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] suggestions similar", os.Args[0])
	fmt.Fprint(os.Stderr, " -text STRING")
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Return saved queries semantically similar to the given text (embedding-based). Requires embeddings to be enabled.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -text STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "suggestions similar --text \"Sapiente aperiam iste quos et sed est.\" --limit 16")
}

func suggestionsAskUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] suggestions ask", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Natural language to SQL and report in one step: ask a question, get a generated SELECT, run it, and return the narrative report. Requires LLM.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "suggestions ask --body '{\n      \"connection_id\": \"Similique dolorem quo eligendi quia ex.\",\n      \"question\": \"cf0\"\n   }'")
}

func suggestionsChatUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] suggestions chat", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Multi-turn Ask conversation with short context memory and optional follow-up suggestions.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "suggestions chat --body '{\n      \"connection_id\": \"Et occaecati numquam molestias harum qui.\",\n      \"question\": \"f\",\n      \"session_id\": \"Illum assumenda.\"\n   }'")
}

func suggestionsExplainUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] suggestions explain", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Explain a SQL query in plain English (one or two sentences). Requires LLM.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "suggestions explain --body '{\n      \"sql\": \"ql\"\n   }'")
}

// reportsUsage displays the usage of the reports command and its subcommands.
func reportsUsage() {
	fmt.Fprintln(os.Stderr, `Report generation and management`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] reports COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    generate: Generate a narrative report from a SQL query`)
	fmt.Fprintln(os.Stderr, `    get: Get a report by ID`)
	fmt.Fprintln(os.Stderr, `    list: List generated reports`)
	fmt.Fprintln(os.Stderr, `    similar: Find reports semantically similar to input text`)
	fmt.Fprintln(os.Stderr, `    rewrite: Rewrite an existing report narrative using a user instruction.`)
	fmt.Fprintln(os.Stderr, `    create-share: Create or refresh a shareable read-only token link for a report.`)
	fmt.Fprintln(os.Stderr, `    get-shared: Fetch a report by valid share token.`)
	fmt.Fprintln(os.Stderr, `    list-shares: List active and revoked share links for a report. Never returns the raw token, only non-secret metadata.`)
	fmt.Fprintln(os.Stderr, `    revoke-share: Revoke a share link so it can no longer be used to fetch its report.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s reports COMMAND --help\n", os.Args[0])
}
func reportsGenerateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports generate", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Generate a narrative report from a SQL query`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports generate --body '{\n      \"connection_id\": \"Sit sit dolorem quia at earum iste.\",\n      \"saved_query_id\": \"311d6455-9936-43d4-9608-50c2d0cedfe2\",\n      \"sql\": \"5x\"\n   }'")
}

func reportsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports get", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get a report by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports get --id \"a05cb3be-34bc-4142-9fd0-132833f3acce\"")
}

func reportsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports list", os.Args[0])
	fmt.Fprint(os.Stderr, " -saved-query-id STRING")
	fmt.Fprint(os.Stderr, " -connection-id STRING")
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprint(os.Stderr, " -offset INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List generated reports`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -saved-query-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -connection-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)
	fmt.Fprintln(os.Stderr, `    -offset INT32: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports list --saved-query-id \"a75c4437-6b13-409c-9865-c265d5238c0a\" --connection-id \"Corporis nam nisi.\" --limit 37 --offset 1459850715")
}

func reportsSimilarUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports similar", os.Args[0])
	fmt.Fprint(os.Stderr, " -text STRING")
	fmt.Fprint(os.Stderr, " -connection-id STRING")
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Find reports semantically similar to input text`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -text STRING: `)
	fmt.Fprintln(os.Stderr, `    -connection-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports similar --text \"ms\" --connection-id \"Et sed.\" --limit 14")
}

func reportsRewriteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports rewrite", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Rewrite an existing report narrative using a user instruction.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports rewrite --body '{\n      \"instruction\": \"u\",\n      \"report_id\": \"79a98ace-7db5-4913-a5d8-9f9bdac69f3d\"\n   }'")
}

func reportsCreateShareUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports create-share", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create or refresh a shareable read-only token link for a report.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports create-share --body '{\n      \"expires_in_hours\": 671,\n      \"report_id\": \"502f4057-058e-457a-9fb4-36784daa4644\"\n   }'")
}

func reportsGetSharedUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports get-shared", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Fetch a report by valid share token.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports get-shared --token \"8e9\"")
}

func reportsListSharesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports list-shares", os.Args[0])
	fmt.Fprint(os.Stderr, " -report-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List active and revoked share links for a report. Never returns the raw token, only non-secret metadata.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -report-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports list-shares --report-id \"3f6e868e-1890-49b4-888c-40142a4d3e2a\"")
}

func reportsRevokeShareUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] reports revoke-share", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Revoke a share link so it can no longer be used to fetch its report.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Share link ID (not the token)`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "reports revoke-share --id \"d3a9e519-b06d-4f51-9b35-55377f1b8c23\"")
}

// schedulesUsage displays the usage of the schedules command and its
// subcommands.
func schedulesUsage() {
	fmt.Fprintln(os.Stderr, `Scheduled report generation and delivery`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] schedules COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List implements list.`)
	fmt.Fprintln(os.Stderr, `    create: Create implements create.`)
	fmt.Fprintln(os.Stderr, `    update: Update implements update.`)
	fmt.Fprintln(os.Stderr, `    delete: Delete implements delete.`)
	fmt.Fprintln(os.Stderr, `    run-now: RunNow implements run_now.`)
	fmt.Fprintln(os.Stderr, `    list-runs: ListRuns implements list_runs.`)
	fmt.Fprintln(os.Stderr, `    retry-run: RetryRun implements retry_run.`)
	fmt.Fprintln(os.Stderr, `    list-deliveries: ListDeliveries implements list_deliveries.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s schedules COMMAND --help\n", os.Args[0])
}
func schedulesListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schedules list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List implements list.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schedules list")
}

func schedulesCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schedules create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create implements create.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schedules create --body '{\n      \"connection_id\": \"Atque debitis similique quia illum reprehenderit.\",\n      \"destination_target\": \"Dolores molestiae iure molestiae.\",\n      \"destination_type\": \"Provident est similique minima.\",\n      \"enabled\": true,\n      \"interval_expr\": \"A expedita et iure.\",\n      \"name\": \"r\",\n      \"saved_query_id\": \"036e0ead-7475-4a1d-8f36-e61d00295e84\",\n      \"sql\": \"z3g\",\n      \"timezone\": \"A minima dolor placeat incidunt.\"\n   }'")
}

func schedulesUpdateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schedules update", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update implements update.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schedules update --body '{\n      \"connection_id\": \"Voluptas consequatur et totam qui.\",\n      \"destination_target\": \"Aliquid reprehenderit qui.\",\n      \"destination_type\": \"Quo enim voluptas.\",\n      \"enabled\": false,\n      \"interval_expr\": \"Ab beatae nam voluptatem alias.\",\n      \"name\": \"mm\",\n      \"saved_query_id\": \"bee739b4-b5af-4b07-810a-1492eb7fb943\",\n      \"sql\": \"1ex\",\n      \"timezone\": \"Earum et voluptas veniam.\"\n   }' --id \"94c4b627-6786-437f-a5f8-ad4f8a20c179\"")
}

func schedulesDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schedules delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Delete implements delete.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schedules delete --id \"b246a1d3-3895-473f-a28c-2ec5a23c81df\"")
}

func schedulesRunNowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schedules run-now", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `RunNow implements run_now.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schedules run-now --id \"7b08c7c8-1c41-4b6a-81ca-352925f8ff73\"")
}

func schedulesListRunsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schedules list-runs", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `ListRuns implements list_runs.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schedules list-runs --id \"675079b3-6556-407a-ac92-8af39e8c029f\"")
}

func schedulesRetryRunUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schedules retry-run", os.Args[0])
	fmt.Fprint(os.Stderr, " -run-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `RetryRun implements retry_run.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -run-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schedules retry-run --run-id \"28606999-67e9-4b9b-aea1-04bf7c3588ed\"")
}

func schedulesListDeliveriesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] schedules list-deliveries", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `ListDeliveries implements list_deliveries.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "schedules list-deliveries")
}

// dashboardsUsage displays the usage of the dashboards command and its
// subcommands.
func dashboardsUsage() {
	fmt.Fprintln(os.Stderr, `Dashboard CRUD and widget resolution`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] dashboards COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List implements list.`)
	fmt.Fprintln(os.Stderr, `    create: Create implements create.`)
	fmt.Fprintln(os.Stderr, `    get: Get implements get.`)
	fmt.Fprintln(os.Stderr, `    update: Update implements update.`)
	fmt.Fprintln(os.Stderr, `    delete: Delete implements delete.`)
	fmt.Fprintln(os.Stderr, `    resolve: Resolve implements resolve.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s dashboards COMMAND --help\n", os.Args[0])
}
func dashboardsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] dashboards list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List implements list.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "dashboards list")
}

func dashboardsCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] dashboards create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create implements create.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "dashboards create --body '{\n      \"name\": \"q8g\"\n   }'")
}

func dashboardsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] dashboards get", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get implements get.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "dashboards get --id \"0cc037b5-4de7-4029-b44d-477db0d40ed1\"")
}

func dashboardsUpdateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] dashboards update", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update implements update.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "dashboards update --body '{\n      \"name\": \"el\",\n      \"widgets\": [\n         {\n            \"position\": 746709202,\n            \"refresh_seconds\": 1450548254,\n            \"report_id\": \"61a62a63-ea45-42fe-9fb6-339e2aac7d9e\",\n            \"saved_query_id\": \"f46a08f3-d0fd-41a9-bcf0-a3881435a32c\",\n            \"title\": \"Porro cupiditate molestiae vitae illum.\",\n            \"widget_type\": \"Et nulla.\"\n         },\n         {\n            \"position\": 746709202,\n            \"refresh_seconds\": 1450548254,\n            \"report_id\": \"61a62a63-ea45-42fe-9fb6-339e2aac7d9e\",\n            \"saved_query_id\": \"f46a08f3-d0fd-41a9-bcf0-a3881435a32c\",\n            \"title\": \"Porro cupiditate molestiae vitae illum.\",\n            \"widget_type\": \"Et nulla.\"\n         }\n      ]\n   }' --id \"a7ddd231-93e5-4927-afc8-8c6cfe544633\"")
}

func dashboardsDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] dashboards delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Delete implements delete.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "dashboards delete --id \"51762a48-6751-42ac-bbd6-1278b98a4c11\"")
}

func dashboardsResolveUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] dashboards resolve", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Resolve implements resolve.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "dashboards resolve --id \"c86ceedc-5c14-4566-abb7-fb9702e765f8\"")
}

// queriesUsage displays the usage of the queries command and its subcommands.
func queriesUsage() {
	fmt.Fprintln(os.Stderr, `Query execution and management`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] queries COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    run: Execute a SQL query and return results`)
	fmt.Fprintln(os.Stderr, `    stat-statements: List top queries from pg_stat_statements (read-only observability)`)
	fmt.Fprintln(os.Stderr, `    explain-plan: Run EXPLAIN (FORMAT JSON) on a read-only query and return plan analysis`)
	fmt.Fprintln(os.Stderr, `    compare-plans: Compare before and after execution plans for a query rewrite or index change`)
	fmt.Fprintln(os.Stderr, `    list-saved: List saved queries`)
	fmt.Fprintln(os.Stderr, `    save: Save a query`)
	fmt.Fprintln(os.Stderr, `    get-saved: Get saved query by ID`)
	fmt.Fprintln(os.Stderr, `    delete-saved: Delete a saved query`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s queries COMMAND --help\n", os.Args[0])
}
func queriesRunUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] queries run", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Execute a SQL query and return results`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "queries run --body '{\n      \"connection_id\": \"Ea eligendi sit.\",\n      \"limit\": 505,\n      \"sql\": \"h8\"\n   }'")
}

func queriesStatStatementsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] queries stat-statements", os.Args[0])
	fmt.Fprint(os.Stderr, " -order-by STRING")
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprint(os.Stderr, " -connection-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List top queries from pg_stat_statements (read-only observability)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -order-by STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)
	fmt.Fprintln(os.Stderr, `    -connection-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "queries stat-statements --order-by \"total_time\" --limit 17 --connection-id \"Sed doloribus hic sit commodi maiores qui.\"")
}

func queriesExplainPlanUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] queries explain-plan", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Run EXPLAIN (FORMAT JSON) on a read-only query and return plan analysis`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "queries explain-plan --body '{\n      \"analyze\": false,\n      \"connection_id\": \"Error quam modi eveniet ut placeat.\",\n      \"sql\": \"x\"\n   }'")
}

func queriesComparePlansUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] queries compare-plans", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Compare before and after execution plans for a query rewrite or index change`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "queries compare-plans --body '{\n      \"after_sql\": \"bg\",\n      \"analyze\": false,\n      \"before_sql\": \"w\",\n      \"binds\": [\n         \"Numquam necessitatibus vel dolor.\",\n         \"Qui deserunt saepe.\"\n      ],\n      \"connection_id\": \"Praesentium sed.\",\n      \"verify_results\": true\n   }'")
}

func queriesListSavedUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] queries list-saved", os.Args[0])
	fmt.Fprint(os.Stderr, " -tags JSON")
	fmt.Fprint(os.Stderr, " -connection-id STRING")
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprint(os.Stderr, " -offset INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List saved queries`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -tags JSON: `)
	fmt.Fprintln(os.Stderr, `    -connection-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)
	fmt.Fprintln(os.Stderr, `    -offset INT32: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "queries list-saved --tags '[\n      \"Est nihil hic eum soluta.\",\n      \"Blanditiis non est.\",\n      \"Deserunt et aut assumenda.\"\n   ]' --connection-id \"Unde aut.\" --limit 64 --offset 1921613477")
}

func queriesSaveUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] queries save", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Save a query`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "queries save --body '{\n      \"connection_id\": \"Nam consequuntur ut ad aut quos.\",\n      \"description\": \"tgs\",\n      \"name\": \"re8\",\n      \"sql\": \"n\",\n      \"tags\": [\n         \"Excepturi velit quidem voluptatibus aliquid et velit.\",\n         \"Harum voluptas consectetur aut est.\",\n         \"Aliquid aliquid.\"\n      ]\n   }'")
}

func queriesGetSavedUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] queries get-saved", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get saved query by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "queries get-saved --id \"d03de24a-be02-4589-b0e0-13e3f4911b72\"")
}

func queriesDeleteSavedUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] queries delete-saved", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Delete a saved query`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "queries delete-saved --id \"82053155-fe1a-412e-8c50-24dc8bbb5d44\"")
}

// investigationsUsage displays the usage of the investigations command and its
// subcommands.
func investigationsUsage() {
	fmt.Fprintln(os.Stderr, `Query Investigation workflow: analyze expensive queries with evidence-backed reports`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] investigations COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    create: Start a new query investigation from SQL or pg_stat_statements context`)
	fmt.Fprintln(os.Stderr, `    create-from-regression: Open an investigation from a regression alert (EXPLAIN + link alert to investigation)`)
	fmt.Fprintln(os.Stderr, `    list: List query investigations for the current organization`)
	fmt.Fprintln(os.Stderr, `    get: Get a query investigation with evidence`)
	fmt.Fprintln(os.Stderr, `    add-candidate: Add a candidate rewrite and compare plans`)
	fmt.Fprintln(os.Stderr, `    update-fix: Advance the fix lifecycle (proposed -> verified -> applied) and record a PR/ticket link. Marking 'applied' snapshots the query's current latency so the poller can confirm the fix.`)
	fmt.Fprintln(os.Stderr, `    suggest-rewrite: Suggest candidate SQL rewrites from the investigation SQL and plan findings (AST-based; no demo scenarios required)`)
	fmt.Fprintln(os.Stderr, `    rank-candidates: Generate rewrite and index-DDL candidates, dry-EXPLAIN rewrites, project index cost (hypopg or honest heuristic), and rank by cost/partitions`)
	fmt.Fprintln(os.Stderr, `    generate-report: Generate an engineering investigation report`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s investigations COMMAND --help\n", os.Args[0])
}
func investigationsCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Start a new query investigation from SQL or pg_stat_statements context`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations create --body '{\n      \"analyze\": true,\n      \"calls\": 6544119297023173194,\n      \"connection_id\": \"Molestias facere dolores aut perferendis doloremque sit.\",\n      \"mean_time_ms\": 0.5935006929907958,\n      \"queryid\": \"Ratione ex iste.\",\n      \"rows\": 7411238645166853964,\n      \"sql\": \"35\",\n      \"title\": \"38\",\n      \"total_time_ms\": 0.0007688444681518402\n   }'")
}

func investigationsCreateFromRegressionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations create-from-regression", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Open an investigation from a regression alert (EXPLAIN + link alert to investigation)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations create-from-regression --body '{\n      \"regression_alert_id\": \"c6338213-566a-4870-8559-fdb89bdca258\"\n   }'")
}

func investigationsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations list", os.Args[0])
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprint(os.Stderr, " -offset INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List query investigations for the current organization`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)
	fmt.Fprintln(os.Stderr, `    -offset INT32: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations list --limit 54 --offset 1477563791")
}

func investigationsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations get", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get a query investigation with evidence`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations get --id \"808c6086-7abe-4dcc-9a36-896691816b70\"")
}

func investigationsAddCandidateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations add-candidate", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Add a candidate rewrite and compare plans`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations add-candidate --body '{\n      \"analyze\": false,\n      \"binds\": [\n         \"Repellendus ea quis debitis.\",\n         \"Unde eaque vitae explicabo.\"\n      ],\n      \"candidate_sql\": \"7k9\",\n      \"verify_results\": true\n   }' --id \"0e2d929d-a462-4ac4-833e-06d4dee84ae6\"")
}

func investigationsUpdateFixUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations update-fix", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Advance the fix lifecycle (proposed -> verified -> applied) and record a PR/ticket link. Marking 'applied' snapshots the query's current latency so the poller can confirm the fix.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations update-fix --body '{\n      \"fix_reference\": \"ox5\",\n      \"fix_status\": \"abandoned\"\n   }' --id \"653acb6c-5616-463f-893d-17841aaf4495\"")
}

func investigationsSuggestRewriteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations suggest-rewrite", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Suggest candidate SQL rewrites from the investigation SQL and plan findings (AST-based; no demo scenarios required)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations suggest-rewrite --id \"30eec620-305d-448d-a72e-f2d75587a800\"")
}

func investigationsRankCandidatesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations rank-candidates", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Generate rewrite and index-DDL candidates, dry-EXPLAIN rewrites, project index cost (hypopg or honest heuristic), and rank by cost/partitions`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations rank-candidates --body '{\n      \"analyze\": true\n   }' --id \"18734116-6da8-4f6c-9a67-4ab0369cd55c\"")
}

func investigationsGenerateReportUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] investigations generate-report", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Generate an engineering investigation report`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "investigations generate-report --id \"a33a4086-2261-40f2-87d0-19643430527f\"")
}

// workspaceUsage displays the usage of the workspace command and its
// subcommands.
func workspaceUsage() {
	fmt.Fprintln(os.Stderr, `Workspace overview, regression inbox, demo scenarios, and security trust`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] workspace COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    overview: PostgreSQL evidence summary for the landing dashboard`)
	fmt.Fprintln(os.Stderr, `    regressions: Regression inbox: queries requiring attention`)
	fmt.Fprintln(os.Stderr, `    acknowledge-regression: Acknowledge a regression alert`)
	fmt.Fprintln(os.Stderr, `    demo-scenarios: Guided demo scenarios for Query Investigation`)
	fmt.Fprintln(os.Stderr, `    security-trust: Security and trust posture for the Security & Trust page`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s workspace COMMAND --help\n", os.Args[0])
}
func workspaceOverviewUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspace overview", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `PostgreSQL evidence summary for the landing dashboard`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspace overview")
}

func workspaceRegressionsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspace regressions", os.Args[0])
	fmt.Fprint(os.Stderr, " -limit INT32")
	fmt.Fprint(os.Stderr, " -include-acknowledged BOOL")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Regression inbox: queries requiring attention`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -limit INT32: `)
	fmt.Fprintln(os.Stderr, `    -include-acknowledged BOOL: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspace regressions --limit 37 --include-acknowledged true")
}

func workspaceAcknowledgeRegressionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspace acknowledge-regression", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Acknowledge a regression alert`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspace acknowledge-regression --id \"41097706-7a35-4e03-ad4a-c64cdafc3f8a\"")
}

func workspaceDemoScenariosUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspace demo-scenarios", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Guided demo scenarios for Query Investigation`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspace demo-scenarios")
}

func workspaceSecurityTrustUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspace security-trust", os.Args[0])
	fmt.Fprint(os.Stderr, " -connection-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Security and trust posture for the Security & Trust page`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -connection-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspace security-trust --connection-id \"Aut officiis iste earum accusantium a.\"")
}
