# Binaries
bin/
/server
/mcp-server
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# goa's scratch output. The app never imports it; `make generate` syncs it into
# api/gen/ (the single committed tree) so `go build ./...` works on a clean clone.
/gen/
goa*/
internal/db/queries/
vendor/
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local editor/MCP config (user-specific paths)
.cursor/mcp.json

# Environment & Secrets
.env
.env.local
.env.*.local
.env.production
deploy/docker/.env.production
*.secret
secrets/

# Build artifacts
coverage.out
coverage.html
*.log
*.tmp

# Frontend dependencies and build output. Every consumer — Dockerfile, CI, the
# release workflow and `make build-frontend` — runs `npm ci`, which deletes
# node_modules and reinstalls from package-lock.json, then `npm run build` to
# regenerate dist. Neither was ever read from the repository; 11,147 committed
# node_modules files were 94% of the tree, and the committed dist was stale
# output from an older build.
node_modules/
frontend/dist/

# npm cache when NPM_CONFIG_CACHE points inside the repo
.npm-cache/

# Cache
.gomodcache/
pgquerynarrative_data/
tools/db/.cache/
tools/db/.venv-nyc/

# Documentation sites
docs-site/
site/

# Local-only planning / marketing (never publish)
release-2.md
.planning/
docs/private/
docs/ROADMAP.md
docs/BACKLOG.md
docs/PHASE2_SUMMARY.md
docs/POSTGRES_CREDIBILITY.md
docs/DEMO_RUNBOOK.md
docs/ops/PRODUCTION.md
docs/ops/INCIDENT_RUNBOOKS.md
docs/linkedin/
cmd/linkedin-social/
automation/
tools/demo/capture_readme_gif.sh
docs/assets/demo-workflow.gif

# Local-only AI agent context (never publish — keep on disk for Cursor/agents)
AGENTS.md
.cursor/rules/product-direction.mdc
/openapi-ts
